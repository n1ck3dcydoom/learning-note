package algorithm.search.binary;

/**
 * Created by n!Ck
 * Date: 2019-02-14
 * Time: 15:44
 * Description:
 * 二分查找的总
 *
 * 其大致模板如下：
 * // 这里必须是 <=
 * while (left <= right) {
 *     int mid = (left + right) / 2;
 *     if (array[mid] ? key) {
 *         //... right = mid - 1;
 *     }
 *     else {
 *         // ... left = mid + 1;
 *     }
 * }
 * return xxx;
 *
 * 其重点在于确定边界值
 */
public class Summary {
}
